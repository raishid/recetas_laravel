<template>
    <div>
        <span class="like-btn" @click="Likereceta"></span>
    </div>
</template>

<script>
export default {
    props: ['recetaId'],
    methods: {
        Likereceta(){
            axios.post('/recetas/' + this.recetaId)
                .then(respuesta =>{
                    console.log(respuesta)
                })
                .catch(error =>{
                    console.log(error)
                });
        }
    }
}
</script>
